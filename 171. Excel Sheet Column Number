class Solution {
public:
    int titleToNumber(string s) {
        int n=s.size();
        int res=0;
        int tmp;
        for(int i=0;i<n;++i)
        {
            tmp=s[i]-'A'+1;
            res=res*26+tmp;
        }
        return res;
    }
};
